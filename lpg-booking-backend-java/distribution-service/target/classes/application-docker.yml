spring:
  datasource:
    url: jdbc:mysql://mysql:3306/lpg_distribution?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    password: ${DB_PASSWORD}

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
