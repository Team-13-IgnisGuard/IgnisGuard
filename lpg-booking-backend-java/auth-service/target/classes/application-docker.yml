spring:
  datasource:
    url: jdbc:mysql://mysql:3306/lpg_auth?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    password: ${DB_PASSWORD}
  rabbitmq:
    host: rabbitmq
    port: 5672

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/
